// @ts-check
import { defineConfig } from "astro/config";

// Ajusta a tu repo real cuando lo subas a GitHub.
export default defineConfig({
  site: "https://roedwin-afk.github.io",
  base: "/comercial-josue/",
});